# Angular client and Express server base structure for simple

## Development server

<b>client</b>:

ng serve --port \<number>

<b>server</b>:

nodemon --watch 'src/**/*.ts' --exec 'ts-node' src/server.ts
